{
  "name": "sendspin-player",
  "version": "1.0.0",
  "description": "Sendspin player web interface dependencies",
  "license": "AGPL-3.0-or-later",
  "private": true,
  "scripts": {
    "build": "mkdir -p static/vendor && npx tailwindcss -i ./src/tailwind.css -o ./static/vendor/tailwind.min.css --minify && cp node_modules/htmx.org/dist/htmx.min.js static/vendor/",
    "postinstall": "npm run build"
  },
  "dependencies": {
    "htmx.org": "^1.9.10"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.1"
  }
}

