{
  "name": "sendspin-player",
  "version": "1.0.0",
  "description": "Sendspin player web interface dependencies",
  "private": true,
  "scripts": {
    "build": "mkdir -p static/vendor && cp node_modules/@picocss/pico/css/pico.min.css static/vendor/ && cp node_modules/htmx.org/dist/htmx.min.js static/vendor/",
    "postinstall": "npm run build"
  },
  "dependencies": {
    "@picocss/pico": "^1.5.11",
    "htmx.org": "^1.9.10"
  }
}

